# RAG Telecom Customer Care Chatbot - Python 3.11+ (tested on 3.13)
# Install into the project-local .venv only:
#   uv venv .venv --python 3.13 && uv pip install -r requirements.txt
#   (or) python -m venv .venv && .venv/Scripts/pip install -r requirements.txt

# --- LangChain core + LCEL ---
langchain>=0.3.27
langchain-core>=0.3.75
langchain-community>=0.3.27

# --- Vector store ---
chromadb>=1.0.0
langchain-chroma>=0.2.4

# --- Local embeddings (all-MiniLM-L6-v2, no external API) ---
# torch must come from the CPU index: on Linux the default PyPI wheel is the
# CUDA build and drags in several GB of nvidia-* packages, which blows the
# Streamlit Community Cloud resource limit. The +cpu local version sorts above
# the plain one, so pip prefers it. Harmless on Windows/macOS.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
langchain-huggingface>=0.3.0
sentence-transformers>=3.0.0

# --- LLM provider ---
langchain-groq>=0.3.0

# --- Ingestion ---
pypdf>=5.0.0

# --- UI / CLI ---
streamlit>=1.40.0
python-dotenv>=1.0.1
